<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--button-->

    <style name="TemaBase" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorControlHighlight">@color/colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">@dimen/calc.TextButtonStyle.Size</item>
    </style>

    <style name="botones" parent="TemaBase">
        <item name="colorButtonNormal">@color/colorAccent</item>
    </style>

    <style name="botonC" parent="TemaBase">
        <item name="colorButtonNormal">@color/BotonC</item>
    </style>

    <style name="Operadores" parent="TemaBase">
        <item name="colorButtonNormal">@color/color.operadores</item>
    </style>

    <style name="Igual" parent="TemaBase">
        <item name="colorButtonNormal">@color/color.igual</item>
    </style>

</resources>
